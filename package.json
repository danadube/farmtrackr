{
  "name": "farmtrackr-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd farmtrackr-web && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}

