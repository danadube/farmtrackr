# Environment variables for FarmTrackr Web App
# Copy this file to .env.local for local development

# Next.js Configuration
NEXT_PUBLIC_APP_NAME=FarmTrackr
NEXT_PUBLIC_APP_DESCRIPTION=Farm CRM Application

# Database
# DATABASE_URL="postgresql://username:password@localhost:5432/farmtrackr"

# Google Sheets API (for transaction import from Google Sheets)
# Get from: https://console.cloud.google.com/apis/credentials
# Required for: Google Sheets import feature
GOOGLE_SHEETS_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_SHEETS_CLIENT_SECRET=your-client-secret-here
GOOGLE_SHEETS_REDIRECT_URI=http://localhost:3000/api/google/oauth/callback

# OpenAI API (for AI Commission Sheet Scanner)
# Get from: https://platform.openai.com/api-keys
# Required for: AI-powered commission sheet scanning feature
OPENAI_API_KEY=sk-your-openai-api-key-here

# Authentication (for future use)
# NEXTAUTH_URL="https://your-domain.vercel.app"
# NEXTAUTH_SECRET="your-secret-key"

# File Upload (for future use)
# UPLOAD_MAX_SIZE=10485760
# ALLOWED_FILE_TYPES="pdf,doc,docx,xlsx,csv"

# Analytics (for future use)
# GOOGLE_ANALYTICS_ID="your-ga-id"

# zipForm Integration (for C.A.R. forms - Coming Soon)
# Get from: https://developer.zipform.com/ or your zipForm Partner account
# Required for: C.A.R. form generation feature
# Status: Coming Soon - See docs/integrations/car-zipform-docusign.md
ZIPFORM_BASE_URL=
ZIPFORM_CLIENT_ID=
ZIPFORM_CLIENT_SECRET=
ZIPFORM_ACCOUNT_ID=
ZIPFORM_REDIRECT_URI=http://localhost:3000/api/integrations/zipform/callback
ZIPFORM_WEBHOOK_SECRET=  # Optional: for webhook signature verification

# DocuSign Integration (for e-signatures - Coming Soon)
# Get from: https://developers.docusign.com/
# Required for: Electronic signature feature
# Status: Coming Soon - See docs/integrations/car-zipform-docusign.md
DOCUSIGN_BASE_URL=https://demo.docusign.net/restapi  # Use https://www.docusign.net/restapi for production
DOCUSIGN_ACCOUNT_ID=
DOCUSIGN_INTEGRATOR_KEY=
DOCUSIGN_USER_ID=  # GUID for JWT authentication
DOCUSIGN_OAUTH_BASE=https://account-d.docusign.com  # Use https://account.docusign.com for production
DOCUSIGN_RSA_PRIVATE_KEY_PATH=./secrets/docusign_private.key  # Path to RSA private key for JWT
DOCUSIGN_WEBHOOK_SECRET=  # Optional: HMAC secret for webhook verification

# App Configuration (for integrations)
APP_BASE_URL=http://localhost:3000  # Use https://your-domain.vercel.app for production
WEBHOOK_PUBLIC_URL=http://localhost:3000/api/integrations/webhooks/docusign  # Public URL for webhooks
DOCUMENT_BUCKET=s3://crm-docs-prod  # Or your preferred object storage path

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
# 1. Copy this file: cp env.example .env.local
# 2. Edit .env.local and replace "your-*_here" with actual values
# 3. Make sure .env.local is in .gitignore (already configured)
# 4. Restart development server after creating .env.local
# 5. For production, add these variables in Vercel dashboard:
#    - OPENAI_API_KEY stays secure on the server (used in /api/ functions)
#    - GOOGLE_SHEETS_* are used server-side only
#    - ZIPFORM_* and DOCUSIGN_* are used server-side only
#    - DOCUSIGN_RSA_PRIVATE_KEY should be stored securely (use Vercel secrets or similar)
# 6. For zipForm/DocuSign integration setup:
#    - See docs/integrations/car-zipform-docusign.md for detailed instructions
#    - These integrations are "Coming Soon" - stubs are in place
# ============================================
